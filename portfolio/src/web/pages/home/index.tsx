import { PageWrapper } from "../../shared/wrapper";

/**
 * 
 * @param props 
 * @returns 
 */
const Home = (props: any) => {
  return (
    <PageWrapper id="home">
      
    </PageWrapper>
  );
};
export default Home;
