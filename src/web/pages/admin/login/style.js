import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  root: {
    width: "100vw",
  },
  loginBox:{
    marginTop:"128px"
  },
  paper:{
    padding:"16px 8px",
    width:"20vw"
  }
}));

export default useStyles;
